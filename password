#include <iostream>
#include <conio.h>
#include<string>
using namespace std;
int main(){
	char amir[64];
	char a;
	bool condi=true,cond=true;
	std::string hatem;
	std::string ha;
	int y=4;
	int x=0;
	//for( x=0;hatem[4]=64;x++){}
	for( x=0 ;condi==true; x++){
		hatem+=_getch();
		char b=hatem[x];
		if( b != '\r'){
			condi=true;}
		else{
			condi=false;}}
		for( x=0 ;cond==true; x++){
		ha+=_getch();
		char b=ha[x];
		if( b != '\r'){
			cond=true;}
		else{
			cond=false;}}
		if(hatem==ha){
		cout<<"right\n";
		}
		else{
			cout<<"wrong\n";}
	/*(int x=0;x<=y;x++){	
		
	}	
	for(int x=0;x<=y;x++){	
		amir[x]=_getch();
	}*//*
	for(int t=0;t<=xy;t++){
		if(amir[t]==hatem[t]){
			condi=true;
	}else
		condi=false;
		}
	if(condi==true)
		printf("right\n");
	else
		printf("wrong\n");
		*/
	
	/*for( x=0;x<=64;x++){	
		cout << hatem[x];}	*/
	//cout << hatem;
		 /*cout << "*";}
		 cout << "\nreenter\n";
		 for(int x=0 ; x <= 64 ; x++){
		 hatem[x]=getch();
		 cout <<"*";}
		 for( x=0;x<=64;x++){
		if(amir[x]==hatem[x]){
			condi=true;
			continue;}
		else{
	cout<<"\nerror\ny";
			condi=false;
			break;}
	if(condi!=false){
		printf("\nture\n");
		}
	}*/
	system("pause");
	return 0 ;
}
